import { Pressable, StyleSheet, Text, View } from 'react-native';
import { brand, radius, space, type } from '../../design';

export default function ForecastCard({ night, prompt, status, onPress }: { night: number; prompt: string; status: string; onPress: () => void }) {
  return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={`Tonight's prompt. ${prompt}`} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.topline}><Text style={styles.eyebrow}>NIGHT {String(night).padStart(2, '0')} FORECAST</Text><View style={styles.pill}><Text style={styles.pillText}>{status}</Text></View></View><Text style={styles.title}>{prompt}</Text><View style={styles.actionRow}><Text style={styles.action}>Enter tonight’s ritual</Text><Text style={styles.arrow}>→</Text></View></Pressable>;
}
const styles = StyleSheet.create({ card: { marginTop: space.lg, minHeight: 235, padding: space.xl, borderRadius: radius.xl, backgroundColor: brand.card, borderWidth: 1, borderColor: 'rgba(235,180,194,0.32)', justifyContent: 'space-between' }, topline: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, eyebrow: { ...type.eyebrow, color: brand.rose, fontSize: 8, letterSpacing: 1.2 }, pill: { paddingHorizontal: 10, paddingVertical: 6, borderRadius: radius.pill, backgroundColor: 'rgba(236,200,133,0.10)', borderWidth: 1, borderColor: 'rgba(236,200,133,0.30)' }, pillText: { ...type.eyebrow, color: brand.gold, fontSize: 7 }, title: { ...type.displayItalic, color: brand.ink, fontSize: 34, lineHeight: 40, marginVertical: space.xl }, actionRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, action: { ...type.bodyStrong, color: brand.ink }, arrow: { color: brand.gold, fontSize: 22 }, pressed: { opacity: 0.78, transform: [{ scale: 0.99 }] } });
