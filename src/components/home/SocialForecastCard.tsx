import { Pressable, StyleSheet, Text, View } from 'react-native';
import { brand, radius, space, type } from '../../design';

export default function SocialForecastCard({ hasMatch, partnerPresent, onPrimary, onSecondary }: { hasMatch: boolean; partnerPresent: boolean; onPrimary: () => void; onSecondary: () => void }) {
  return <View style={styles.card}><View style={styles.orb}><View style={[styles.dot, partnerPresent && styles.dotActive]} /></View><Text style={styles.kicker}>{hasMatch ? 'SHARED ROOM' : 'CONNECTION'}</Text><Text style={styles.title}>{hasMatch ? (partnerPresent ? 'Another presence is here tonight.' : 'The room is quiet tonight.') : 'Your private world comes first.'}</Text><Text style={styles.body}>{hasMatch ? 'Their words stay private. Presence is the only signal shown here.' : 'Complete your pattern and shelf before a real connection can appear.'}</Text><View style={styles.actions}><Pressable onPress={onPrimary} accessibilityRole="button" style={({ pressed }) => [styles.primary, pressed && styles.pressed]}><Text style={styles.primaryText}>{hasMatch ? 'Open room' : 'Continue setup'}</Text></Pressable><Pressable onPress={onSecondary} accessibilityRole="button" style={({ pressed }) => [styles.secondary, pressed && styles.pressed]}><Text style={styles.secondaryText}>{hasMatch ? 'View safety' : 'How it works'}</Text></Pressable></View></View>;
}
const styles = StyleSheet.create({ card: { marginTop: space.lg, padding: space.xl, borderRadius: radius.xl, backgroundColor: brand.card, borderWidth: 1, borderColor: brand.line }, orb: { width: 48, height: 48, borderRadius: 24, backgroundColor: 'rgba(137,108,181,0.16)', alignItems: 'center', justifyContent: 'center' }, dot: { width: 10, height: 10, borderRadius: 5, backgroundColor: brand.inkFaint }, dotActive: { backgroundColor: brand.rose }, kicker: { ...type.eyebrow, color: brand.purple, fontSize: 8, marginTop: space.lg }, title: { ...type.displayItalic, color: brand.ink, fontSize: 27, lineHeight: 33, marginTop: space.xs }, body: { ...type.body, color: brand.inkMid, marginTop: space.sm }, actions: { flexDirection: 'row', gap: space.sm, marginTop: space.lg }, primary: { flex: 1, minHeight: 48, borderRadius: radius.pill, backgroundColor: brand.purple, alignItems: 'center', justifyContent: 'center' }, primaryText: { ...type.bodyStrong, color: brand.ink }, secondary: { flex: 1, minHeight: 48, borderRadius: radius.pill, borderWidth: 1, borderColor: brand.line, alignItems: 'center', justifyContent: 'center' }, secondaryText: { ...type.bodyStrong, color: brand.inkMid }, pressed: { opacity: 0.72 } });
