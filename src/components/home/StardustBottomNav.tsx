import { Pressable, StyleSheet, Text, View } from 'react-native';
import { CreateIcon } from '../Icons';
import { brand, radius, type } from '../../design';
export default function StardustBottomNav({ onPress }: { onPress: () => void }) { return <View pointerEvents="box-none" style={styles.overlay}><Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel="Open quick actions" style={({ pressed }) => [styles.button, pressed && styles.pressed]}><CreateIcon color={brand.void} size={25} /></Pressable><Text pointerEvents="none" style={styles.label}>Act</Text></View>; }
const styles = StyleSheet.create({ overlay: { position: 'absolute', left: 0, right: 0, bottom: 40, alignItems: 'center', zIndex: 20 }, button: { width: 58, height: 58, borderRadius: radius.pill, backgroundColor: brand.rose, borderWidth: 4, borderColor: brand.card, alignItems: 'center', justifyContent: 'center', shadowColor: brand.rose, shadowOpacity: 0.32, shadowRadius: 14, shadowOffset: { width: 0, height: 6 }, elevation: 10 }, label: { ...type.eyebrow, color: brand.ink, fontSize: 7, marginTop: 2 }, pressed: { opacity: 0.76, transform: [{ scale: 0.94 }] } });
