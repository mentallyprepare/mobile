import { Pressable, StyleSheet, Text, View } from 'react-native';
import { brand, radius, space, type } from '../../design';
import { t } from '../../i18n';
import { useLanguage } from '../../i18n/react';
export default function RecapCard({ completed, streak, onPress }: { completed: number; streak: number; onPress: () => void }) { useLanguage(); return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={t('recap_card.open_a11y')} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><Text style={styles.kicker}>{t('recap_card.kicker')}</Text><Text style={styles.title}>{t('recap_card.title')}</Text><View style={styles.metrics}><View><Text style={styles.value}>{completed}</Text><Text style={styles.label}>{t('recap_card.sealed')}</Text></View><View style={styles.divider} /><View><Text style={styles.value}>{streak}</Text><Text style={styles.label}>{t('recap_card.streak')}</Text></View><View style={styles.divider} /><View><Text style={styles.value}>{Math.max(0, 21 - completed)}</Text><Text style={styles.label}>{t('recap_card.ahead')}</Text></View></View><Text style={styles.action}>{t('recap_card.action')}</Text></Pressable>; }
const styles = StyleSheet.create({ card: { marginTop: space.lg, padding: space.lg, borderRadius: radius.xl, backgroundColor: brand.card, borderWidth: 1, borderColor: brand.line }, kicker: { ...type.eyebrow, color: brand.rose, fontSize: 9, lineHeight: 12 }, title: { ...type.bodyStrong, color: brand.ink, fontSize: 19, lineHeight: 24, marginTop: 3 }, metrics: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginTop: space.xl }, value: { ...type.display, color: brand.ink, fontSize: 30, lineHeight: 34, textAlign: 'center' }, label: { ...type.eyebrow, color: brand.inkLow, fontSize: 8, textAlign: 'center' }, divider: { width: 1, height: 42, backgroundColor: brand.line }, action: { ...type.bodyStrong, color: brand.gold, fontSize: 12, marginTop: space.xl }, pressed: { opacity: 0.76 } });
