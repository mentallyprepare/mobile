import { Pressable, StyleSheet, Text } from 'react-native';
import { brand, radius, space, type } from '../../design';
export default function EducationCard({ onPress }: { onPress: () => void }) { return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel="Learn why notes remain sealed" style={({ pressed }) => [styles.card, pressed && styles.pressed]}><Text style={styles.kicker}>WHY IT WORKS THIS WAY</Text><Text style={styles.title}>Sealing creates a boundary, not a score.</Text><Text style={styles.body}>The ritual preserves a pause between writing and response. It is reflective, private, and non-clinical.</Text><Text style={styles.action}>Read the privacy promise →</Text></Pressable>; }
const styles = StyleSheet.create({ card: { marginTop: space.lg, padding: space.lg, borderRadius: radius.xl, backgroundColor: 'rgba(236,200,133,0.07)', borderWidth: 1, borderColor: 'rgba(236,200,133,0.24)' }, kicker: { ...type.eyebrow, color: brand.gold, fontSize: 9, lineHeight: 12 }, title: { ...type.bodyStrong, color: brand.ink, fontSize: 19, lineHeight: 24, marginTop: space.sm }, body: { ...type.body, color: brand.inkMid, fontSize: 14, lineHeight: 20, marginTop: space.sm }, action: { ...type.bodyStrong, color: brand.rose, fontSize: 12, marginTop: space.lg }, pressed: { opacity: 0.76 } });
