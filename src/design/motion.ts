// Per directive: motion communicates state, never decorates. Reduce Motion
// replaces spatial motion with fades everywhere.

export const duration = {
  instant: 100,
  micro: 180,
  standard: 320,
  expressive: 460,
  ritual: 900,
};
