// Storage keys, kept in their own module so the pure client can import them
// without pulling in react-native.
export const ACCESS_KEY = 'mp.accessToken';
export const REFRESH_KEY = 'mp.refreshToken';
